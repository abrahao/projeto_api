{% extends 'base.html.twig' %}

{% block body %}
    <h1>Editar Empresa</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn">Salvar</button>
    {{ form_end(form) }}
{% endblock %}
